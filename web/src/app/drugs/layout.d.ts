import { ReactNode } from 'react';
interface DrugsLayoutProps {
    children: ReactNode;
}
export default function DrugsLayout({ children }: DrugsLayoutProps): import("react").JSX.Element;
export {};
